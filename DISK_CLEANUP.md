## Disk Space Issue

The following directories need to be deleted to free up disk space:
- `~/projects/femitryon/venv` (300MB+)
- `~/projects/femitryon/.git` (200MB+)

Run the following command to free up space:
```bash
rm -rf ~/projects/femitryon/venv ~/projects/femitryon/.git
```