## Force-Push Required

Due to rebase conflicts, a force-push is required to update the remote repository. Run the following command to proceed:

```bash
cd ~/projects/unpaste && git push -f origin main
```